# Dépendances optionnelles pour l'interface Streamlit.
#
# Ces versions sont volontairement figées ensemble : une version plus récente de
# pyarrow a déjà provoqué un crash binaire à l'import sur cet environnement.
streamlit==1.45.1
pyarrow==15.0.2
altair==5.5.0
plotly>=5.24,<6
protobuf==6.33.6
